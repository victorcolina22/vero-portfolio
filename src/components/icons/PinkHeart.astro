---
interface Props {
  class?: string;
  height?: number;
  width?: number;
}

const { class: customClass, height, width } = Astro.props;
---

<img
  src="/images/decorative/pink-flower-lg.svg"
  alt=""
  class={customClass}
  style={`width: ${width}px; height: ${height}px;`}
/>
